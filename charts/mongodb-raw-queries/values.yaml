namespaceOverride: "mongodb"

architecture: standalone

auth:
  enabled: false

persistence:
  enabled: true

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

service:
  type: ClusterIP