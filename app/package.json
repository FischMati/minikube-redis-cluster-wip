{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "tsc",
    "build:docker": "eval $(minikube docker-env) && docker build --no-cache  --force-rm -t my-node-app .",
    "deploy": "npm run build:docker && kubectl rollout restart deployment/node-app",
    "dev": "ts-node index.ts"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/express": "^5.0.0",
    "@types/mongoose": "^5.11.96",
    "express": "^4.21.0",
    "ioredis": "^5.4.1",
    "mongoose": "^8.7.0",
    "pino": "^9.4.0",
    "pino-pretty": "^11.2.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
